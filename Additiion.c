#include<stdio.h>
int main()
{
    int mashiur, mash,ans;
     printf("there are two numbers:");
    scanf("%d,%d",&mashiur, &mash);
    ans=mashiur+mash;
    printf("Result is:  %d ",ans);
    return 0;
}